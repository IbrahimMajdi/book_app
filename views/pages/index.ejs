<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/base.css">

    <title>BOOKS</title>
</head>

<body>
    <h1>books app</h1>

    <a href="/searches/new"> search books</a>

    <ul>

        <h3> books number :<%= booksNum  %> </h3>

        <% books.forEach(books=> { %>
        <li>
            <img src=<%= books.thumbnail %> />
        </li>
        <li>
            <%= books.title %>
        </li>
        <li>
            <%= books.authorname %>
        </li>
        <li>
            <%= books.description %>
        </li>

        <button> <a href="books/<%=books.id%>"> Details</a></button>

        <% }) %>

    </ul>

    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="/app.js"></script>
</body>

</html>